@model LambdaForums.Models.Forum.ForumIndexModel
<h1>Forums</h1>

<table class=" table table-hover" id="forumIndexTable">
    <tbody>

        @foreach (var forum in Model.ForumLists)
        {
            <tr>
                <td>
                    <a asp-controller="Forum" asp-action="Post" asp-route-Id="@forum.Id">
                        @forum.Title
                    </a>

                </td>
                <td>
                    @forum.Description
                </td>
            </tr>

        }
    </tbody>
</table>

